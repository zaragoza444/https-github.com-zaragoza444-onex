// Override when hosting wallet UI separately from shiva-bridge (e.g. GitHub Pages).
// Example: window.SHIVA_BRIDGE_URL = 'https://api.your-domain.com';
window.SHIVA_BRIDGE_URL = window.SHIVA_BRIDGE_URL || '';
